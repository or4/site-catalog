import { colorsScheme, backgroundColorsScheme, fontFamilyScheme, fontSizeScheme } from '../schemes';

export const amountOnPage = {
  text: {
    color: colorsScheme.amountOnPage,
    background: backgroundColorsScheme.amountOnPage,
    fontFamily: fontFamilyScheme.amountOnPage,
    fontSize: fontSizeScheme.amountOnPage,
    lineHeight: '28px',
  }
};